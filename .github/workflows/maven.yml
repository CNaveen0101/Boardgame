name: mycustompipelines

on:
  push:
    branches: [ main ]

jobs:
  setup-install:
   runs-on: myrunner
   steps:
   - name: Checkout code
     uses: actions/checkout@v6

   - name: Set up java
     uses: actions/setup-java@v3
     with:
      distribution: 'temurin'
      java-version: '11'
 
  Test:
    runs-on: myrunner
    needs: setup-install
    steps:
    - name: unit test
      run: mvn test

    - name: SonarQube Scan
      uses: SonarSource/sonarqube-scan-action@v7.0.0
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

